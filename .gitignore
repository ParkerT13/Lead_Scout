__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# HubSpot knowledge base — contains real email addresses, generated locally
# hubspot_knowledge_public.json is committed (sanitized — no real emails)
emailer/hubspot_knowledge.json

# API credentials — fill in credentials.json.example and rename to credentials.json
# Share this file directly with colleagues; never commit it
credentials.json
